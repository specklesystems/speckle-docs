version: 2.1

workflows:
  version: 2
  build:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/node:current
    steps:
      - checkout
      - run:
          command: yarn build
